name: getx_news_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.5
  http: ^0.13.5
  flutter_config: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^2.0.0
  mockito: ^5.4.0

flutter:
  uses-material-design: true
